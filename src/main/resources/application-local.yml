server:
  servlet:
    contextPath: /transfermoney/api
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/database_teste
    driverClassName: com.mysql.cj.jdbc.Driver
    username: root
    password:
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MariaDBDialect
    hibernate:
      ddl-auto: update
stripe:
  api-key: